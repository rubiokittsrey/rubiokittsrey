import { ThemeToggle } from '@/components/features/controls';

export default function Controls() {
    return (
        <div className="flex items-center space-x-6">
            <ThemeToggle />
        </div>
    );
}
